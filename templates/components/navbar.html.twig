<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <i class="fas fa-calendar-alt me-2"></i>Gestion Congés
        </a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav me-auto">
                {% if is_granted('ROLE_EMPLOYE') %}
                    <li class="nav-item">
                        <a class="nav-link {{ app.current_route == 'app_employe_dashboard' ? 'active' : '' }}" 
                           href="{{ path('app_employe_dashboard') }}">
                            <i class="fas fa-home me-1"></i> Tableau de bord
                        </a>
                    </li>
                {% endif %}
                
                {% if is_granted('ROLE_MANAGER') %}
                    <li class="nav-item">
                        <a class="nav-link {{ app.current_route starts with 'app_manager_' ? 'active' : '' }}" 
                           href="{{ path('app_manager_dashboard') }}">
                            <i class="fas fa-tasks me-1"></i> Validation
                        </a>
                    </li>
                {% endif %}
            </ul>
            
            <ul class="navbar-nav">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="userDropdown" data-bs-toggle="dropdown">
                            <i class="fas fa-user-circle me-1"></i> {{ app.user.email }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li>
                                <a class="dropdown-item" href="{{ path('app_profile') }}">
                                    <i class="fas fa-user me-2"></i> Mon profil
                                </a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                                    <i class="fas fa-sign-out-alt me-2"></i> Déconnexion
                                </a>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">
                            <i class="fas fa-sign-in-alt me-1"></i> Connexion
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

<style>
.navbar {
    padding: 0.8rem 1rem;
    margin-bottom: 2rem;
}
.nav-link {
    transition: all 0.2s;
}
.nav-link:hover {
    transform: translateY(-2px);
}
.dropdown-menu {
    border: none;
    box-shadow: 0 0.5rem 1rem rgba(0,0,0,0.15);
}
</style>
